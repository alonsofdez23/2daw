'use strict'

//Cargamos todo el HTML primero.
window.addEventListener('load', () => {

    //Capturamos el boton.
    var botongenerador = document.querySelector("#generador");
    //Capturamos el evento de click sobre el boton para llamar a generarFormulario de auxiliar.
    botongenerador.addEventListener('click', () => {
        generarFormulario();
    });


















});